services:
  db:
    image: postgis/postgis:16-3.4
    environment:
      POSTGRES_DB: sitesofgrace
      POSTGRES_USER: sog
      POSTGRES_PASSWORD: local-dev-only-change-me
    ports:
      - "5432:5432"
    volumes:
      - sog_pgdata:/var/lib/postgresql/data
volumes:
  sog_pgdata: